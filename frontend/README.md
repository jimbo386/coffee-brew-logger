# Frontend

This is the React (Vite) frontend for the Coffee Brew Logger demo.

Run locally:

1. cd frontend
2. npm install
3. npm run dev

The frontend expects the backend to run at http://localhost:8000 by default. To change the API URL set VITE_API_URL in a .env file in the frontend directory.
